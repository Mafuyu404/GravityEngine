package cc.sighs.gravityengine.client;

/** Which system currently owns the entity's final global presentation. */
public enum PresentationAuthority {
    BODY_ATTITUDE,
    GRAVITY_FRAME,
    VANILLA
}
