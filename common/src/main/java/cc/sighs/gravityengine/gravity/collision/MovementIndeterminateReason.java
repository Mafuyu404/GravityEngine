package cc.sighs.gravityengine.gravity.collision;

/** Diagnostic provenance captured at the failing stage, never an acceptance policy. */
public enum MovementIndeterminateReason {
    NONE,
    INITIAL_BODY_QUERY_BUDGET,
    INITIAL_OVERLAP_RECOVERY_BUDGET,
    INITIAL_OVERLAP_RECOVERY_INFEASIBLE,
    INITIAL_OVERLAP_RECOVERY_NO_PROGRESS,
    INITIAL_OVERLAP_RECOVERY_CORRIDOR_INDETERMINATE,
    INITIAL_OVERLAP_RECOVERY_CORRIDOR_BLOCKED,
    INITIAL_OVERLAP_RECOVERY_PASS_LIMIT,
    INITIAL_SUPPORT_QUERY_INDETERMINATE,
    INITIAL_SUPPORT_QUERY_BUDGET,
    COLLISION_QUERY_BUDGET,
    VERTICAL_SWEEP_INDETERMINATE,
    VERTICAL_SWEEP_INITIAL_OVERLAP,
    TANGENT_SWEEP_INDETERMINATE,
    TANGENT_SWEEP_INITIAL_OVERLAP,
    TANGENT_SWEEP_NON_BLOCKING_ZERO_TOI,
    TANGENT_SWEEP_NO_PROGRESS,
    TANGENT_SWEEP_BUMP_LIMIT,
    TERMINAL_SUPPORT_QUERY_INDETERMINATE,
    TERMINAL_SUPPORT_QUERY_BUDGET,
    FINAL_CONTACT_QUERY_INDETERMINATE,
    FINAL_CONTACT_QUERY_BUDGET,
    FINAL_CONTACT_PENETRATION,
    SWEEP_QUERY_INDETERMINATE,
    SWEEP_INITIAL_OVERLAP,
    ADVANCE_NON_BLOCKING_ZERO_TOI,
    ADVANCE_INFEASIBLE,
    ADVANCE_NO_PROGRESS,
    ADVANCE_BUMP_LIMIT,
    OTHER
}
