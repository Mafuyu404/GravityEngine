package cc.sighs.gravityengine.attitude.runtime;

/** Actor presentation/control ownership; reference-aligned characters release immediately. */
public enum BodyAttitudeOwnership {
    INACTIVE,
    ACTIVE
}
